import { defineConfig } from 'vite';

export default defineConfig({
  base: '/whisper-share/',
  worker: {
    format: 'es',
    rollupOptions: {
      output: {
        // Configure the output name for worker scripts
        entryFileNames: 'service-worker.js',
        // Default naming for any chunks or assets generated by the worker build itself
        chunkFileNames: 'assets/worker-chunks/[name]-[hash].js',
        assetFileNames: 'assets/worker-assets/[name]-[hash].[ext]',
      }
    }
  },
  optimizeDeps: {
    exclude: ['@ffmpeg/ffmpeg', '@ffmpeg/util', '@ffmpeg/core']
  },
  server: {
    headers: {
      'Cross-Origin-Embedder-Policy': 'require-corp',
      'Cross-Origin-Opener-Policy': 'same-origin',
    },
    fs: {
      allow: ['..', './node_modules']
    }
  },
  assetsInclude: ['**/*.wasm'],
  build: {
    rollupOptions: {
      output: {
        // Standard Vite naming for the main application's assets
        entryFileNames: 'assets/[name]-[hash].js',
        chunkFileNames: 'assets/[name]-[hash].js',
        assetFileNames: 'assets/[name]-[hash].[ext]',
      },
    },
  },
});
